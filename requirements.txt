# All decoders — Python 3.14 compatible.
torch
opencv-python
numpy
pillow
av
psutil
matplotlib
imageio-ffmpeg
torchaudio
decord
ffmpegcv
deffcode
video-reader-rs
torchcodec
ffmpeg-python
# Nelux (formerly CeLux) is published via GitHub releases, not PyPI.
# Pinned wheel URL — pip auto-selects from URL pattern wouldn't work, so
# the workflow installs the matching wheel for the target Python explicitly.
